#include <iostream>
#include "vector.hpp"

int main() {
	ft::vector<int> v1;
	ft::vector<int> v2(10);
	//ft::vector<int> v3 = v2;
	// ft::vector<int>::iterator it = 0;
	// for ( int i = *it; i < 10; ++i, ++it) {
	// 	std::cout << *it <<std::endl;
	// }
}
